// Top-level build.gradle file
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.2.1' // Use the latest version
        classpath 'com.google.gms:google-services:4.4.1' // Use the latest version
        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }

}


plugins {
    id 'com.google.gms.google-services' version '4.4.1' apply false
}

//allprojects {
//    repositories {
//        google()
//        jcenter() // Note: jcenter is deprecated, consider replacing with mavenCentral()
//        // mavenCentral() // Uncomment this if you replace jcenter
//    }
//}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}




